# --- LLM ---
anthropic>=0.39

# --- Broker + market data ---
alpaca-py>=0.30
yfinance>=0.2
exchange-calendars>=4.5

# --- Data / analysis ---
pandas>=2.1
numpy>=1.26
scipy>=1.11
statsmodels>=0.14
pandas-ta
quantstats>=0.0.62
vectorbt>=0.26

# --- Options pricing (optional but used by options backtester) ---
QuantLib>=1.33

# --- Config / I/O ---
pydantic>=2.5
pydantic-settings>=2.1
python-dotenv>=1.0
python-frontmatter>=1.0
PyYAML>=6.0
structlog>=24.1
apscheduler>=3.10,<4.0

# --- Native dependencies (optional, install separately) ---
# TA-Lib>=0.4.28        # Requires C library: brew install ta-lib (macOS) or apt install libta-lib-dev (Linux)

# --- Dev ---
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=4.1
ruff>=0.4
mypy>=1.8
