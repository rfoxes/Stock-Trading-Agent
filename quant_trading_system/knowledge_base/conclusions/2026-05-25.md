# 2026-05-25 — Day 13 (Memorial Day, US markets closed), 13th do-nothing run; NEW operator files appeared in `state/` (manual.md + tasks.md); set-active deferred due to broker/instructions conflict

## Summary of what I did today

- Read `state/last_handoff.md` (written 2026-05-22 by Fri-me), `state/summary.md` (still empty), and `state/active_strategy.md` (still the seed unset state from day 1).
- Discovered **two new files** in `state/`, both with mtime `2026-05-25 17:08`:
  - `state/manual.md` — a new operator manual describing a substantially refactored harness (use system `python3` directly; trading happens only via `python3 -m quant_trading_system.cli execute`; orders no longer submitted by the orchestrator directly).
  - `state/tasks.md` — a focused to-do list for today's Claude that says "Pick an active strategy if none is set. Conservative day-1: prefer `equity_trend_following_ema_cross` if regime is bullish… After `set-active`, run `execute`."
- Re-read the most recent conclusion (`2026-05-22.md`) for context.
- Confirmed `ATTENTION.md` mtime is still **`2026-05-14 17:44`** — unchanged for 9 weekdays. The operator placed the two new `state/` files but did NOT touch `ATTENTION.md`.
- Verified the new CLI: `python3 -m quant_trading_system.cli --help` lists `execute` and `update-script` as expected by manual.md. System `python3` is now the documented interpreter (not the `.venv` symlink). All probe subcommands work.
- Ran the minimum probe via system `python3`:
  - `account` → `equity=$111,167.72, cash=-$96,531.22, buying_power=$14,636.50, day_trade_count=0`. Equity is **+$1,736.36 (+1.59%)** vs. Fri's $109,431.36 across the three-day weekend — surprising on a closed Memorial Day. Marks moved up across every name (SPY 744.41 → 750.66 +0.8%; NVDA 214.61 → 217.31 +1.3%; TSLA 423.90 → 429.30 +1.3%; etc.). Buying-power rose by the same +$1,736.36 (mechanical). Either Alpaca paper marked-to-market against Sun-night futures / extended-hours quotes, or the paper-broker mark service rolled forward despite the cash session being closed. The SPY after-hours quote (`bid=723.82, ask=768.65, mid=746.235`) is byte-identical to Fri's stale quote, so the official quote feed has NOT updated. The interpretation: position marks are slightly drifted from a holiday mark service, but no fresh tape exists.
  - `positions` → **same 10 longs, same lot sizes, same avg-entry prices to the cent** as every day since day 5: AAPL 72@271.296944, AMZN 76@248.534868, GOOGL 56@338.790179, JPM 64@313.036719, META 28@681.554643, MSFT 44@421.235909, NVDA 96@199.397083, QQQ 28@647.963929, SPY 35@708.813143, TSLA 48@403.981458. Net unrealized P&L **+$11,168**, vs. Fri's +$9,431 (+$1,737 today on holiday marks).
  - `open-orders` → `[]`.
  - `recent-trades` → `count=0`. Journal still empty.
  - `market-status` → `is_open=false, next_open_iso=2026-05-26T09:30:00-04:00`. Confirmed Memorial Day closure; first fresh-tape session is **Tuesday 5/26 09:30 ET**. `now_iso=2026-05-26T00:55:50+00:00` ≈ 17:55 PT — the scheduler ticked on time.
  - `get-active` → `strategy_id=""`. Still seed-unset.
  - `regime` → `bull, confidence=0.76, sma_200_slope=+0.000765, price_vs_sma200=+9.85%, sma_50_vs_200=+2.62%, adx=25.77, realized_vol=0.1017`. Indicators rolled forward slightly off Fri's (price_vs_sma200 widened 9.39% → 9.85%; realized_vol eased 0.106 → 0.1017; adx ticked up 25.54 → 25.77). Same bull classification, indistinguishable for strategy-selection purposes.
- Read `strategies/equity/trend_following_ema_cross/strategy.md` and `strategy.py`. The strategy file is consistent with the regime (trending + bullish). Confirmed the runtime's default `watchlist` resolves to **exactly the 10 symbols already on the broker:** `['SPY','QQQ','AAPL','MSFT','GOOGL','AMZN','NVDA','META','TSLA','JPM']`. The strategy's **exit pass iterates `ctx.positions` and can submit market sells** if any name shows a death-cross or `ADX(14)<20`.
- **No `set-active` call. No `execute` call. No trades submitted. No edits to any strategy `.md` or `.py` file. No edit to `ATTENTION.md`, `state/summary.md`, or `state/active_strategy.md`.** I did write today's conclusion (this file), a new `state/tasks.md`, and a new `state/last_handoff.md`. I did NOT modify `state/manual.md` (operator-owned long-lived doc).

## Observations and reasoning

**The major new signal today is the operator's two new files, not the market.** Markets were closed for Memorial Day, the broker book composition is byte-identical to Friday's, and the regime classifier rolled forward in a normal direction. The only real new information is that the operator placed `state/manual.md` and `state/tasks.md` at `17:08` (just before the 16:00 PT = 23:00 UTC scheduler tick missed by ~minus-an-hour-and-some). This is the first operator-driven file change in 9 weekdays — `ATTENTION.md` remains untouched, but the new manual/tasks pair is the operator's clearest direct instruction since 5/14.

**Why I did NOT follow tasks.md literally and call `set-active equity_trend_following_ema_cross` + `execute` today.** The tasks.md says "Pick an active strategy if none is set" and recommends `equity_trend_following_ema_cross` in a bull regime. But two facts make literal compliance unsafe right now:

1. **The tasks.md's own Status section asserts a precondition that is FALSE.** It says "Broker state to verify: account flat, no open orders." Reality: 10 unattributed long-equity positions worth ~$208K market value, ~+$11.2K unrealized. The status block is also labelled "(One line each. Filled in by yesterday's Claude.)" — but yesterday's Claude (me, on 5/22) wrote a long handoff explicitly documenting all 10 positions. So that section was NOT filled in by yesterday's Claude; it was placed by the operator as a template that assumes a clean baseline. The precondition is wrong-by-construction, not stale.

2. **`equity_trend_following_ema_cross`'s exit pass would iterate all 10 broker positions on its first run.** The runtime's default watchlist is exactly `[SPY, QQQ, AAPL, MSFT, GOOGL, AMZN, NVDA, META, TSLA, JPM]` — a byte-for-byte match for the unattributed book. Entry pass skips names we already hold (so it'd be empty), but the exit pass calls `crossed_below(ema_fast, ema_slow)` and `adx < 20` checks against `ctx.positions` directly. If any of those 10 names triggers either exit on Tuesday's bars, the strategy submits a market sell tagged with `strategy_id=equity_trend_following_ema_cross`, retroactively attributing the operator's manual book to a harness strategy I just chose — exactly the "manual-override violation" the prior 12 handoffs and the original task file warned against. META is at -10.1% unreal today; that's well outside any normal trend-follow tolerance and would be a strong sell candidate against the wrong owner.

The two facts compound: the operator's template assumes I'm starting from flat (so any exit-pass intent would target only positions I just opened), but the actual book is someone else's, so the same code path would mass-flatten operator-owned positions on Tuesday's open.

**The conservative reading reconciles both signals.** The new manual.md explicitly says: "Doing nothing is a valid outcome and is often the correct one. The harness rewards stability." And it says: "If the broker is unreachable, do not modify strategies. Document the outage in `last_handoff.md` and stop." There's no equivalent "if the broker has unattributed positions, do not set-active" rule, but the principle ("when the world doesn't match the precondition, document and stop") is identical, and the prior 12 days of handoffs have followed the same logic without harm.

**There are two clean paths for the operator to unblock the next run, either of which I'd happily follow:**

- **Path A — flat-close the book first.** If the operator wants tasks.md to apply literally, the 10 unattributed positions need to be closed (or migrated to a separate account) so the strategy's exit pass starts from `ctx.positions == []`. Then `set-active equity_trend_following_ema_cross` + `execute` is safe: entry pass on the watchlist, no risk of overwriting operator P&L.
- **Path B — explicitly assign the book.** If the operator wants the harness to take ownership of the existing positions, they can write a one-line addition to `state/tasks.md` saying e.g. "Attribute the existing 10 longs to `equity_trend_following_ema_cross`; let the strategy manage exits per its own rules." Then tomorrow's Claude reads that, calls `set-active` with a reason that cites the assignment, and `execute` runs as designed — exits per strategy logic become legitimate (and probably trigger immediately on META given the -10% unreal). That's a deliberate operator choice; I shouldn't make it for them.

A third path I considered and rejected: **silently edit the strategy's `watchlist` parameter** to disjoint it from the existing book (e.g., a different symbol universe). That would let me set-active + execute without touching the 10 positions on day 1, but it materially changes the strategy's intent for a meta-reason and would be exactly the kind of cosmetic-or-worse churn the manual warns against. Strategy edits should reflect strategy lessons, not orchestration hacks.

**Memorial Day reality reinforces deferral.** Even if the precondition conflict were resolved, today is a market holiday — `is_open=false`, no fresh tape, SPY quote byte-identical to Friday's stale after-hours quote. The strategy script would compute against bars whose latest entry is Friday's close, and any intents would queue for Tuesday's open. There is zero cost to deferring the set-active decision one more business day so tomorrow's Claude can see the operator's response to today's surfaced conflict (or fresh state/ files), plus Tuesday's actual open tape. Fri-me's handoff already telegraphed Monday as a low-information run; that prediction held cleanly.

**The +1.59% holiday equity move is mildly anomalous but not actionable.** Equity rose +$1,736 on a closed-market day; marks on every name moved up 0.4%–1.3%. The SPY official quote is unchanged (still the Friday stale `bid=723.82, ask=768.65, mid=746.235`), so this isn't a "tape opened anyway" surprise — it's the paper broker's mark-to-market rolling forward against some non-public reference (futures, ETH, or a holiday roll service). The book composition (lot sizes, avg-entry prices) is byte-identical to Friday, so no trades occurred. Documenting in case future-me wants to investigate the paper-broker mark service; no action this run.

**Why I did NOT update `state/manual.md`'s "Recent feedback" section.** The manual itself says that section is for durable lessons learned (multi-week / multi-month). Today's lessons are very specific to this transition (operator placed new manual + tasks; tasks.md precondition mismatched broker reality on day 1 of the new framework). That's a one-time event, not a long-running operating rule. If the same precondition mismatch recurs after the operator's resolution, that would justify a one-line feedback entry like "Always cross-check tasks.md preconditions against actual broker state before set-active." For now, the observation lives in this conclusion and in `last_handoff.md`.

**Carry-forward caveats from prior days (still unchanged):**
- IEX-only volume coverage → absolute-volume strategies under-trigger. Trend-following / momentum strategies are unaffected. (Notable because `equity_trend_following_ema_cross` is `relative` — would not have been impacted by this caveat even if `execute` had run.)
- `health`/`portfolio-health` equity-id mismatch bug — would have returned `strategy_not_found` if I'd tried `health equity_trend_following_ema_cross`. Skipped, expected nulls anyway.
- `.venv/bin/python` still broken; manual.md explicitly authorises system `python3`. Documented, not a blocker.
- The two stale-after-hours-quote pattern for SPY (~$45 spread) continues; quote feed clearly does not refresh outside cash session.

**Summary line for the running record.** Day 13 (Memorial Day, US markets closed) was a do-nothing run — the harness's 13th in a row — but the *reason* changed today. For the first time since `ATTENTION.md`, the operator placed direct instructions in `state/` (manual.md + tasks.md) that fundamentally shift the harness's workflow (no more `submit`; `execute` is the trading path). The tasks.md's day-1 precondition ("account flat") doesn't match the still-unattributed 10-position book, and the recommended strategy (`equity_trend_following_ema_cross`) would have its exit pass iterate that exact book. Deferring `set-active` for one more business day so the operator can resolve the precondition (Path A: flat-close; Path B: explicit attribution in tasks.md) is the right move. Market is closed, regime is unchanged, book composition is byte-identical to Fri, mark-to-market rose +1.59% on holiday adjustments only. Next fresh-tape tick is Tuesday 5/26 post-close.
