# News tasks for the next run

Yesterday's news agent writes this. Replace, don't append.

---

## Status as of the last update

(Defaults below apply for the first run. Yesterday's agent should fill in
real observations.)

- **Last brief assessment:** none — this is the first scheduled news run.
- **Universe last covered:** seed watchlist only; no held-position lookup yet.
- **Notable carry-forwards from yesterday:** none.

## To do today

1. Run the standard workflow (news_manual.md §"Workflow").
2. **First run:** confirm `cli news-fetch` and `cli news-universe` both return
   clean JSON; confirm files appear under `knowledge_base/news/`. If they
   don't, document it in today's brief and stop.
3. Watch for FOMC / CPI / jobs report announcements this week and flag in
   the brief.

## Open questions for the operator

(none yet)
