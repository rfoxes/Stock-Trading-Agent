# Weekly research log — 2026-07-11 (Saturday)

> **Commit note:** all research work below was performed on Saturday 2026-07-11; the run reached the git-sync
> step on Monday 2026-07-13 (the session spanned the weekend), so the commit is prefixed `[research 2026-07-13]`
> while the log is dated to when the work was done. No trader run intervened between the two — last commit before
> this was `[trader 2026-07-10]`, and the working tree held exactly the 7/11 mutations. State re-verified intact
> before commit (9 strategies, 32/32 claimed, provisional_count 4).

## TL;DR

- **1 CONVERSION applied: WULF → `equity_rsi_divergence`** (unrestricted triage winner Sharpe **0.880 on 12
  trades** ≥ baseline 0.5). Released from provisional `equity_event_driven_catalyst`; provisional marker cleared
  by hand (harness Open Q#8). **provisional_count 5 → 4.**
- **Adds / updates / archives: 0 / 0 / 0.** No new candidate is backtestable on the single-symbol / daily /
  next-open engine that isn't a data-blocked, trade-floor-blocked, or update-variant of an existing strategy
  (5th consecutive no-op on additions — engine constraints unchanged).
- **Remaining 4 provisionals:** QCOM HOLD (thin, 3 trades), SYNA HOLD (thin, 4 trades), SKHY HOLD (0 bars, IPO),
  SPCX ESCALATED (19 bars < 60, structural). None past `revalidate_by` yet.
- **Archive sweep: all 8 active-set equity strategies → KEEP.** `event_driven_catalyst` is now archive-evaluable
  (3 `trade_closed` from the 7/8→7/9 AVGO/MU/ORCL exits) and passed KEEP. `trend_following` healthy (rolling
  Sharpe 6.25, PSR 0.87, 10 trades).
- **Infra healthy:** `.venv/bin/python3` works, bare `python3` still dead (Homebrew 3.14.5, no deps),
  `.git-sync-queue/` clean (only June-1 test markers → LaunchAgent processing normally), `gap-registry
  coverage_holes: []`.
- **Library unchanged at 20 strategies** (12 equity incl. `equity_watch_only`, 8 options), all `active`, 0 archived.

---

## PRIORITY ZERO — provisional revalidation (mandatory-attach doctrine)

Ran **unrestricted `cli triage-symbol <SYM> --no-claim`** on all 5 (no `--gap-type` so every active+testing equity
strategy scores; `--no-claim` so held/escalated ones don't get their `revalidate_by` auto-extended — manual
feedback 2026-07-08). Window 2024-07-11 → 2026-07-11.

| Symbol | Incumbent (provisional) | Unrestricted winner | Sharpe / trades | Verdict |
|---|---|---|---|---|
| **WULF** | `equity_event_driven_catalyst` | `equity_rsi_divergence` | **0.880 / 12** | **CONVERT** ✓ |
| QCOM | `equity_event_driven_catalyst` | `equity_mean_reversion_bollinger` | 0.817 / **3** | HOLD (thin <5) |
| SYNA | `equity_pairs_trading_cointegration` | `equity_rsi_divergence` | 0.731 / **4** | HOLD (thin <5) |
| SPCX | `equity_trend_following_ema_cross` | — (all errored) | 19 bars < 60 | ESCALATED (structural) |
| SKHY | `equity_watch_only` | — (all errored) | 0 bars | HOLD (no history, IPO) |

### WULF — CONVERTED to `equity_rsi_divergence`
- Unrestricted triage: `rsi_divergence` **0.8797 Sharpe, 12 trades, max_dd −5.3%, total_return +16.2%** — clears
  baseline AND ≥5 trades. Runner-up `momentum_macd_histogram` 0.369/26tr (below baseline); `breakout_volume`
  0.163/9tr. Incumbent `event_driven_catalyst` = 0 trades (un-backtestable), so per manual feedback 2026-07-08 the
  "≥5-trade" guard applies to the CHALLENGER only, which it clears at 12 → convert.
- **Action taken (applied verbatim, not recommended):**
  1. `cli add-active equity_event_driven_catalyst --replace --symbols AVGO,MU,ORCL,QCOM` — released just WULF while
     preserving event_driven's live reconciled claims (AVGO/MU/ORCL) + its still-held QCOM provisional.
  2. `cli triage-symbol WULF` (auto-claim) → `verdict: claimed`, `equity_rsi_divergence` on WULF.
  3. `cleared_prior_library_gap: false` and `provisional_count` stayed 5 → confirmed the Open Q#8 cross-strategy
     bug bit again; called `memory.clear_provisional_claim('WULF')` → `True`, `provisional_count 5 → 4`.
- WULF starts trading Monday 7/13 under `rsi_divergence` (which also holds HPE). This is the THIRD confirmation of
  the 2026-07-08 "gap-type-filter artifact" lesson: WULF looked un-validatable as an `event_catalyst` provisional
  (its only registered responder, `event_driven_catalyst`, does 0 trades by construction), but unrestricted triage
  found a real momentum/reversion responder with a genuine 12-trade sample.

### QCOM — HELD (thin, unchanged)
- Winner `mean_reversion_bollinger` 0.817 but only **3 trades** (<5 → fluke-prone; a 3-trade Sharpe is not a real
  Sharpe). Runner-up `rsi_divergence` 0.096/6tr (below baseline). Identical picture to 7/08.
- `revalidate_by 2026-07-21` **preserved** (scored `--no-claim`, no auto-extension). Deadline has NOT passed
  (today 7/11 < 7/21). HOLD again. Not structurally blocked — just thin. Re-check next Saturday 7/18; if still <5
  trades AND 7/21 has passed, escalate as "borderline: clears baseline on <5 trades — accept thin sample or relax
  the num_trades requirement? (operator)".

### SYNA — HELD (thin, unchanged)
- Winner `rsi_divergence` 0.731 but only **4 trades** (<5). Runner-up `mean_reversion_bollinger` 0.486/4tr.
  Identical to 7/08 picture.
- `revalidate_by 2026-07-21` preserved. Deadline not passed. HOLD. Same escalation trigger as QCOM if still thin
  after 7/21. (Note: SYNA is `pairs_cointegration`'s ONLY claim; if it eventually converts to `rsi_divergence`,
  `pairs_cointegration` ends with an empty claim list — that is fine, it stays a library strategy; do not archive
  it: `active`, un-backtestable, no journal evidence → archive battery = KEEP.)

### SPCX — ESCALATED (structural, unchanged)
- `simulate equity_trend_following_ema_cross --symbol SPCX` → **19 bars < 60** (was 17 on 7/8; ~+1/week). Every
  candidate errors `insufficient bars`. Un-backtestable until ~60 bars accrue (~September/October).
- This is a permanent structural block, not "needs more time to validate a signal." Re-triage cannot clear it.
  Remains escalated (Open Q#7) + quarantined. `revalidate_by 2026-07-21` preserved (`--no-claim`); the ORIGINAL
  deadline is recorded so the lapse stays auditable. Never auto-remove.

### SKHY — HELD on `equity_watch_only` (no history, IPO)
- `triage-symbol SKHY --no-claim` → `true_library_gap`, all candidates error `insufficient bars (0 < 60)`. SK
  Hynix's permanent ticker begins regular-way trading **Monday 7/13**, so it still had 0 bars today.
- Correct no-history fallback (open issue #5): routed to `equity_watch_only` (the honest "watching, not trading"
  grade). `revalidate_by 2026-07-24` — NOT passed. HOLD. **Structural note:** like SPCX, SKHY needs ~60 trading
  days of bars (~mid-October) to become backtestable, so its 7/24 deadline will lapse with <60 bars. Flagged for
  the operator alongside SPCX (fixed non-auto-extending accrual deadline, or accept the long quarantine).

**Provisionals after this run: 4** — QCOM/SPCX/SYNA (`revalidate_by 2026-07-21`), SKHY (`2026-07-24`).

---

## Gaps identified this week (from trader tasks.md, last_handoff.md, carried from 7/9–7/10 briefs)

All tagged `responder: NONE` by the trader — informational, not tradable under the mandate. Nearly all are
**overlay primitives / claim-broadening / new-category** items, i.e. NOT standalone `propose-strategy` additions
(Open Q#4, the single biggest gap-clearing blocker). Listed for visibility; none clearable by a research addition
on the current engine:

1. **Provisional/quarantined validations (PRIORITY ZERO)** — worked above; WULF converted, 4 remain.
2. **Index-inclusion / forced-flow / IPO liquidity-rotation** — SK Hynix (SKHY) $26.5B Nasdaq debut; SPCX
   Nasdaq-100 forced-flow; BlackRock QQQ-challenger ETF. No rule reads index-inclusion/forced-flow mechanics; the
   taxonomy still has no `index_rebalance` gap_type. Architectural (Open Q#4).
3. **Regulatory / agency-action overlay** — META EU DSA (preliminary, on the held name); NHTSA AV warning
   (TSLA/GOOGL/AMZN); EU DMA App Store; SEC reporting overhaul. No rule reads a court/agency action. Overlay.
4. **Capital-allocation / capex-commitment overlay** — MU $250B US buildout; META 14GW / in-house "Iris" chip;
   Lutnick pushing Samsung/SK Hynix US fabs. No rule reads a multi-year capex/reshoring announcement. Overlay.
5. **Earnings/print-window ASSIGNMENT gap** — JPM (7/14, window OPEN), TSLA (7/22), ARM (7/23), INTC (7/23; JPM
   Q3 short-call), AMZN (7/30). All claimed by trend/breakout, NOT `event_driven_catalyst`/`long_straddle_earnings`.
   A category-assignment / earnings-window overlay, not a new template.
6. **`event_driven_catalyst` exit calibration** — is `max_hold_days: 7` right? Researched (see below): PEAD drift
   window is 5–20 trading days, so 7 is defensible (short end). Un-backtestable (news signal non-replayable →
   0 trades) so `evaluate-update` can't adjudicate; the 3 live closes (n=3) are too few to recalibrate on. HOLD.
7. **M&A-arb activation** — RKLB (acquirer) / IRDM (target, $54/sh); SYNA/onsemi. `pairs_cointegration` declares
   `pairs_arbitrage` but the RKLB/IRDM pair is unmodeled (both currently breakout). Needs a spread/2nd-leg model
   (Open Q#5). Not addable on single-symbol sim.
8. **Litigation / product / vendor-strategy events** — AAPL v. OpenAI; MSFT dropping OpenAI/Anthropic in
   Excel/Outlook; export-control report. No rule reads a lawsuit/product launch/vendor shift. Overlay.
9. **Activist-short / short-call** — BE Hunterbrook rebuttal; INTC JPM Q3 short-call. No responder. Overlay.
10. **Vol-regime activation** — record tech single-name IV (23-yr high) vs benign VIX ~17. Structures exist
    (`iron_condor_high_iv`, `long_straddle_earnings`, `jade_lizard`, `calendar_spread`) but none claims a universe
    symbol and all are options → un-backtestable without a chain-data fixture. Dispersion, not index vol.
11. **Fallback-threshold question (issue #5)** — no-history now routes to `equity_watch_only` (SKHY, correct). Open
    case is only the degenerate 0-trade Sharpe-0.0 backtest routing to a below-baseline *trading* provisional
    (was WULF/QCOM/SYNA). Note: WULF's conversion this week shows the fix is unrestricted triage, not routing.
12. **First-pass / head-to-head validations** — DELIBERATELY NOT RUN (degenerate 0-trade tiebreak, Open Q#3;
    manual feedback 2026-06-16). Record and hold.

---

## Archive sweep — all KEEP (0 archives)

`cli evaluate-archive` on all 8 active-set trading equity strategies (`equity_watch_only` excluded — passive,
never trades). Thresholds: 90-day rolling window, min rolling Sharpe 0.0, min rolling PSR 0.5, 60-day zero-trades.

| Strategy | Decision | Key metrics |
|---|---|---|
| `equity_event_driven_catalyst` | KEEP | 3 trades in 90d window, rolling Sharpe null, 9 actions/60d — **now archive-evaluable** (3 `trade_closed`); no trigger fired |
| `equity_momentum_macd_histogram` | KEEP | 1 trade/90d, rolling Sharpe null, 12 actions/60d |
| `equity_trend_following_ema_cross` | KEEP | **rolling Sharpe 6.25, PSR 0.87, 10 trades** — healthy |
| `equity_breakout_volume_confirmation` | KEEP | 0 lifetime trading events (deployed 7/8) — "no evidence" |
| `equity_mean_reversion_bollinger` | KEEP | 0 trades/90d, 1 action (deployed 7/8) |
| `equity_rsi_divergence` | KEEP | 2 lifetime events, 0 trading events |
| `equity_sector_rotation_momentum` | KEEP | 0 lifetime events |
| `equity_pairs_trading_cointegration` | KEEP | 0 lifetime events |

**No ARCHIVE verdict fired**, so no manual inspection of the `order_rejected`-as-evidence false-positive (manual
feedback 2026-06-06) was needed. `event_driven_catalyst` crossed from "no evidence" to "evaluable with evidence"
this week (its 7/9 reconciled closes) and passed KEEP cleanly — the archive machinery now works end-to-end on a
strategy with real closed-trade history.

---

## Web research (rank-2, below gap-clearing)

Ran two curated WebSearch queries; the addition space is confirmed exhausted for the current engine (5th straight
no-op on additions). No WebFetch deep-read was warranted — every hit fell into an already-known block class.

**Query 1 — "post-earnings announcement drift optimal holding period event-driven catalyst exit rule 2025"**
(sources below). Finding: earnings-momentum drift is captured over a **5–20 trading-day window**; exit on signal
decay, stop-loss, or holding-period expiry. Bearing on gap #6: `event_driven_catalyst`'s `max_hold_days: 7` sits
inside the empirical 5–20d window (short end) → **defensible, no recalibration warranted** even if it were
testable. It is NOT testable (enters on a non-replayable `news_brief` signal → 0 trades in every sim), so
`evaluate-update` is degenerate. A PEAD *addition* is separately blocked: needs EPS-surprise fundamentals the
backtester lacks, and quarterly cadence yields <20 trades/symbol (Open Q#2). Foregone (same conclusion as 7/8).

**Query 2 — "Quantpedia novel equity trading strategy 2026 daily bars swing systematic backtest"** (sources
below). Most relevant hit: a June-2026 **trend-filtered short-term reversal** (200-day MA filter + multi-day
pullback trigger, tested on 6 liquid ETFs). Assessment: (a) structurally it's a trend-filtered mean-reversion —
overlaps `equity_mean_reversion_bollinger` and `equity_rsi_divergence`, so by the novelty rule it's an UPDATE
variant (`_v2`), not an addition; (b) a multi-day-pullback-in-uptrend entry is *more* selective than Bollinger
touches, so on single-symbol 2-yr sim it would fire well under the 20-trade floor (the most active strategy tops
out at 16 trades on META). Not worth a manufactured `evaluate-update` (both variants would be thin). Foregone.

**Conclusion:** 0 candidates both backtestable on the single-symbol/daily/next-open engine AND structurally
non-overlapping. Per the manual ("don't run an addition battery on a candidate you wouldn't independently
endorse"), correct no-op. The recurring blockers are unchanged: the 20-trade floor (Open Q#2), the
event/overlay-architecture gap (Open Q#4), and the un-backtestable event/pairs/options families (Open Q#5).

---

## Infra / housekeeping checks

- **Interpreter:** `.venv/bin/python3` (3.13.13) works; bare `python3` → `No module named 'requests'` (Homebrew
  3.14.5). Operator still hasn't repointed the scheduled tasks. Re-flagged (Open Q#1).
- **git-sync queue:** `.git-sync-queue/` holds only the June-1 test markers (`marker_test.json`, `test.txt`,
  `.gitkeep`) — no week-over-week pile-up → LaunchAgent `com.harness.gitrunner` is processing normally.
- **gap-registry:** `coverage_holes: []`; `volatility_regime` responders intact (calendar_spread,
  iron_condor_high_iv, jade_lizard, long_straddle_earnings). No options `gap_types` re-tagging needed (task 4
  from last week — no-op, operator added/removed nothing).
- **Universe:** 32 symbols, 32/32 claimed, `unclaimed_count 0`. No new unclaimed symbol appeared this week
  (SKHY was already triaged by the trader on 7/10).

---

## Changes applied this run

- `active_strategies.md`: `equity_event_driven_catalyst` {AVGO,MU,ORCL,QCOM,WULF} → {AVGO,MU,ORCL,QCOM};
  `equity_rsi_divergence` {HPE} → {HPE,WULF}.
- `provisional_claims.md`: removed WULF. Count 5 → 4.
- **Library membership: unchanged (0 adds / 0 updates / 0 archives).**
- Manual: no edit (no new durable threshold-calibration lesson; WULF is a clean re-application of the existing
  2026-07-08 unrestricted-triage + manual-clear doctrine).

## Open questions for next Saturday (carried; full list in research_tasks.md)

- QCOM / SYNA: HELD thin (3 / 4 trades). If still <5 after `revalidate_by 2026-07-21`, escalate the
  "accept thin sample vs relax num_trades" policy call to the operator.
- SPCX (19 bars) and SKHY (0→~5 bars by 7/18): both need ~60 bars (~Oct) to become backtestable; their deadlines
  (7/21, 7/24) will lapse first. Operator options: fixed non-auto-extending accrual deadline, reassign gap_type,
  or accept the long quarantine. Never auto-remove.
- Open Q#8 (cross-strategy claim doesn't auto-clear the provisional marker) bit again on WULF — worked around by
  hand. Still needs the harness fix.
