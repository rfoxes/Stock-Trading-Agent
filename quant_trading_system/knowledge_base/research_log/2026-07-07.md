# Weekly research log — 2026-07-07

**(Nominal Saturday 2026-07-04 run, executing Tue 2026-07-07 after the July 4
holiday weekend. First research run since 2026-06-20 — the 6/27 and 7/04
Saturdays produced no log; this run covers the ~2.5-week gap.)**

Interpreter: `.venv/bin/python3` (3.13.13) for the entire run — bare `python3`
is still Homebrew 3.14.5 and dies at context-build (`No module named 'requests'`).
Open Q#1, unresolved for weeks. CLI healthy otherwise.

## TL;DR

**Adds 0 / Updates 0 / Archives 0 — a clean, correct no-op run.** All library
work this week is provisional-claim revalidation + escalation. No candidate was
`propose-strategy`-shaped; every logged gap is architecture-shaped (overlay
primitive / assignment blocked by an un-backtestable responder / options-not-
backtestable / new-category-needed) — all already escalated. Active set
**UNCHANGED**; no head-to-heads run (the pending ones are degenerate first-pass
validations per the manual's 2026-06-16 rule).

Library: 19 strategies (11 equity, 8 options), all `active`, 0 archived.
`gap-registry coverage_holes: []`. Active set: 8 strategies, universe 26,
claimed 26, `unclaimed_count 0`, `provisional_count 3`.

## PRIORITY ZERO — provisional claims (the core work this week)

Worked `state/provisional_claims.md` first, per doctrine. Re-triaged all three.
**None cleared baseline; all three remain execution-quarantined.** The unifying
finding: **all three are structurally un-validatable by the current
triage/backtest machinery, and re-triaging them auto-extends `revalidate_by`,
masking the lapse** (last week's Open Q#7, now confirmed for all three, not just
SPCX).

| Symbol | Strategy (gap_type) | Re-triage 7/07 | Orig. deadline | Auto-bumped to | Root block |
|---|---|---|---|---|---|
| **SPCX** | trend_following (`volatility_regime`) | `provisional_claim`, sharpe `null` | **2026-07-04 (LAPSED)** | 2026-07-21 | 16 bars (<60 floor); gap_type has only options responders → `backtester does not support options strategies (no chain data)` |
| **QCOM** | event_driven_catalyst (`event_catalyst`) | `provisional_claim`, Sharpe 0.0 / 0 trades | 2026-07-10 | 2026-07-21 | sole responder `event_driven_catalyst` is un-backtestable (enters on non-replayable `news_brief` signal → 0 trades in every sim) → **can never clear baseline via triage** |
| **SYNA** | pairs_cointegration (`pairs_arbitrage`) | `provisional_claim`, Sharpe 0.0 / 0 trades | 2026-07-10 | 2026-07-21 | sole responder needs the short-leg (short ON) modeled; single-symbol `simulate` returns 0 trades → **can never clear baseline via triage** |

- **SPCX — ESCALATED (deadline lapsed).** Its original `revalidate_by` (2026-07-04)
  has objectively passed and it still cannot be ranked. Per doctrine ("still below
  baseline AND `revalidate_by` has passed → ESCALATE; keep quarantined; never
  auto-remove"), it is escalated to the operator under Open Q#7 below and stays
  PROVISIONAL/quarantined. Concrete state: SPCX joined the Nasdaq-100 **today
  (Tue 7/7, ~$4.3B forced passive buying)**, but it now has only **16 trading
  bars** (up from 5 on 6/20) — it needs ≥60 before *any* equity backtest, i.e.
  ~early September at ~1 bar/session. Its gap_type `volatility_regime` has only
  options responders the backtester can't run. **Doubly, permanently stuck**; a
  ~Sept re-check on bar count is the earliest it could even be ranked.

- **QCOM & SYNA — deadline (7/10) 3 days out, so not yet strictly escalation-due,
  but structurally identical to SPCX.** Both re-triaged to unchanged
  `provisional_claim` (Sharpe 0.0). Their sole responders can NEVER produce a
  clearing Sharpe under the single-symbol backtester (event_driven is
  un-backtestable; pairs needs a second leg). And my mandatory re-triage today
  **auto-bumped both deadlines 7/10 → 7/21**, so next Saturday's file will show
  7/21 and the "deadline passed" trigger will NOT fire — the exact Open-Q#7
  masking bug, now confirmed for all three. I kept both quarantined and folded
  them into the consolidated structural escalation (Open Q#7). I did NOT
  hand-promote, did NOT auto-remove, did NOT let either trade.

## Gaps identified this week (from 7/6 news brief + 7/3 tasks.md)

`gap-registry coverage_holes` is **empty**; every item below is an
activation / assignment / taxonomy / overlay gap, NOT a registry hole. All were
`responder: NONE` (or degenerate). None is a standalone, backtestable,
single-symbol equity strategy that could pass the addition battery — they are
the SAME classes escalated as Open Q#4/#5 for 4+ weeks.

- **event_catalyst overlay cluster (the bulk):** regulatory/antitrust rulings
  (GOOGL EU €4.1B Android fine upheld + Klarna/Yelp; META 29-state suit),
  restructuring/workforce (MSFT thousands; **ORCL 21k — held name, −18.69%,
  book's largest drawdown**), capital-return (JPM $50B buyback+dividend; MU
  $250M), pricing/margin (AAPL ~55% hikes; "extreme memory shortage"
  pass-through), product-line/business-model (NVDA startup-cloud+rev-share; AMZN
  own AI chips), short-interest/positioning (Burry AI-bear on NVDA/TSLA),
  litigation on a held name (MU DRAM price-fixing class action). → needs an
  **event-window overlay primitive**; blocked by `event_driven_catalyst` being
  un-backtestable. Not `propose-strategy`-shaped.
- **earnings_window assignment:** TSLA (Q2 del. 480k 7/2; earnings 7/22), JPM
  (earnings 7/14, options window open by 7/6), INTC (7/23) are claimed by
  trend-following, not the earnings-window responder. Reassignment path is
  head-to-head, which is **degenerate** on the un-backtestable event_driven
  responder (manual 2026-06-16). Blocked.
- **index-rebalance / forced-flow:** SPCX→Nasdaq-100 (7/7), SK Hynix listing
  (~7/10). No rule reads an index schedule. Tagged NEW category.
- **macro-event-window:** NFP +57k miss / Fed on hold / core PCE 3.4% / USMCA
  review window. Trader tagged `NEW_CATEGORY_NEEDED`.
- **vol-regime activation:** VIX ~16.6, index IV-rank below the >50 gate; MU
  post-print IV crush, SPCX/TSLA/JPM event-IV. The vol options strategies
  (`iron_condor_high_iv`, `calendar_spread`, `jade_lizard`,
  `long_straddle_earnings`) are declared for `volatility_regime` but none claims
  a universe symbol — and none is backtestable (no chain data), so a claim can't
  be validated via triage/battery. Same structural block as SPCX.

## Archive sweep — all KEEP (0 archives)

Ran `cli evaluate-archive` on all 8 active-set equity strategies:

| Strategy | Decision | Key metrics |
|---|---|---|
| equity_trend_following_ema_cross | KEEP | rolling Sharpe **6.25**, PSR **0.87**, 10 trades — healthy (up from 1.10/0.56/7 on 6/20) |
| equity_momentum_macd_histogram | KEEP | rolling_sharpe null, 1 trade in window, 12 actions — thin evidence |
| equity_event_driven_catalyst | KEEP | rolling_sharpe **null**, **0 trades in window**, 3 actions — still un-adjudicable |
| equity_pairs_trading_cointegration | KEEP | 0 lifetime trading events — "no evidence" |
| equity_breakout_volume_confirmation | KEEP | 0 lifetime trading events — "no evidence" |
| equity_mean_reversion_bollinger | KEEP | rolling_sharpe null, 0 trades in window, 1 action |
| equity_rsi_divergence | KEEP | 2 lifetime events, 0 trading — "no evidence" |
| equity_sector_rotation_momentum | KEEP | 0 lifetime trading events — "no evidence" |

**To-Do #4 (last week) resolved:** despite MU's Q3 print (6/24 AMC),
`equity_event_driven_catalyst` STILL shows `rolling_sharpe: null` / 0 trades in
the rolling window — no closed-trade data point materialized (the strategy fires
on a non-replayable news signal and hasn't produced a closed trade). It remains
un-adjudicable by every battery. KEEP.

## Web research (rank-2) — 2 candidates, both foregone REJECT, NOT run

Per the manual ("don't run a battery on a candidate you wouldn't independently
endorse just to generate activity"), neither was implemented — both are
structurally mis-fit to the single-symbol backtester + the ≥20-trade floor.

1. **Post-Earnings-Announcement Drift (PEAD / SUE effect).** Searched 2026-07-07.
   Ball & Brown (1968); Quantpedia "50 Years in PEAD Research"
   (https://quantpedia.com/50-years-in-pead-research/), "Post-Earnings
   Announcement Effect" (https://quantpedia.com/strategies/post-earnings-announcement-effect),
   Wikipedia PEAD (https://en.wikipedia.org/wiki/Post%E2%80%93earnings-announcement_drift).
   Concrete rules exist (rank by SUE, long top decile / short bottom decile,
   hold 5–20 sessions, rebalance quarterly; ~2.6–9.4%/quarter abnormal return).
   **Foregone REJECT:** the anomaly is *cross-sectional* (needs a decile
   portfolio across many names) and *quarterly* (~4 events/symbol/yr → ~8 over a
   2-yr window). The harness backtester is single-symbol and can't build the
   decile spread, and 8 events is far below the 20-trade floor. It would be a
   candidate I do not endorse → not implemented. (Confirms Open Q#2.)
2. **Index-rebalance / forced-flow front-running.** Searched 2026-07-07.
   Quantpedia "ETF Re-balancing and Hedge Fund Front-Running Trades"
   (https://quantpedia.com/etf-re-balancing-and-hedge-fund-front-running-trades/),
   Nasdaq "Looking at Nasdaq-100 Index Adds and Deletes"
   (https://www.nasdaq.com/articles/looking-nasdaq-100-index-adds-and-deletes),
   AlphaArchitect "Cost of index rebalancing"
   (https://alphaarchitect.com/cost-of-index-rebalancing/). Real effect (~$50B
   Nasdaq-100 reconstitution turnover; documented pre-rebalance drift). **Foregone
   REJECT:** fires 0–1×/symbol (SPCX's single 7/7 add is one event) →
   un-backtestable on single-symbol `simulate`, cannot reach 20 trades. Same
   conclusion as last week's index-rebalance candidate. Not implemented.

## Head-to-head / conflict resolution — none run (intentional)

No new strategy was proposed, so no symbol-claim CONFLICT arose. The trader's
listed head-to-heads (tasks.md) are first-pass VALIDATIONS of uniquely-held
claims — the manual's 2026-06-16 rule says these are **systematically degenerate**
(trend_following "wins" by doing 0 trades; a 1-trade challenger Sharpe isn't a
real Sharpe) and must NOT be churned on while the 0-trade-tiebreak bug (Open Q#3)
stands. None run. Active set UNCHANGED.

## Infra checks

- **Interpreter:** bare `python3` still broken (Homebrew 3.14.5). Used
  `.venv/bin/python3` throughout. Open Q#1.
- **git-sync queue:** `.git-sync-queue/` holds only the Jun-1 test files
  (`.gitkeep`, `marker_test.json`, `test.txt`) — no real markers piling up →
  `com.harness.gitrunner` LaunchAgent processing normally.
- **Research cadence:** 6/27 Saturday produced no log; this is the delayed 7/04
  run. Not a harness failure, but flagging the ~2.5-week gap since 6/20.

## What I did NOT do (and why)

- No adds — no candidate is endorsable / battery-passable (num_trades floor +
  architecture-shaped gaps).
- No updates — no variant proposed.
- No archives — all 8 active-set equity strategies KEEP.
- No head-to-heads — degenerate first-pass validations (Open Q#3).
- No manual "Recent feedback" edit beyond the one durable calibration lesson
  appended this run (provisional auto-extension masking; see manual).
- Did NOT auto-remove or hand-promote any provisional — kept all 3 quarantined.

## Open questions carried to the operator

See `research_tasks.md` — Open Q#1 (interpreter), #2 (num_trades floor), #3
(head-to-head 0-trade tiebreak), #4 (event/overlay architecture), #5
(event_driven un-backtestable), #6 (`_load_news_brief` staleness guard), #7
(**escalated: all 3 provisionals structurally un-validatable + auto-extending
deadline masks the lapse**).
