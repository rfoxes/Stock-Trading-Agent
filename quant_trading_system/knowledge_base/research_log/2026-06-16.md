# Weekly research log — 2026-06-16

*(Saturday 6/13 run; the scheduled task did not complete until Tue 6/16 because
the Python environment was broken — see §0. Dating the log 6/16, the actual
completion date.)*

## TL;DR

- **Library changes: ZERO adds, ZERO updates, ZERO strategy archives.** Correct
  outcome — see reasoning below.
- **One metadata fix applied:** closed the `volatility_regime` registry coverage
  hole by tagging 4 options strategies with `gap_types` (they were the natural
  responders all along but had no `gap_types:` frontmatter, so the registry
  couldn't see them). `gap-registry` now reports `coverage_holes: []`.
- **Archive sweep:** all 11 active equity strategies → KEEP.
- **Head-to-head validation:** ran 11 contested first-pass claims. **No
  defensible reassignments** — every "trend_following wins" verdict is degenerate
  (trend wins by doing 0 trades, or by a 1-trade fluke). Per the manual's
  recent-feedback ("do not act on degenerate adjudication") and open-question #3,
  I did NOT churn the active set. Two genuine negative-fit claims surfaced
  (MSFT/macd, ARM/breakout) — flagged, not reassigned.
- **No strategy ADD:** the one battery-eligible candidate class researched
  (VIX-regime equity) cannot clear the `num_trades ≥ 20` single-symbol floor.
  Same 3-week-old structural blocker. Not faking a battery run on a known-reject.

## §0. Infrastructure: bare `python3` is broken; the harness now runs via `.venv`

This is the most important operational finding of the week.

- On **Jun 11 16:49** Homebrew upgraded `/opt/homebrew/bin/python3` to **Python
  3.14.5**. The new interpreter's site-packages lack nearly every harness
  dependency: `requests`, `alpaca-py`, `pydantic`, `statsmodels`, `yfinance`,
  `python-frontmatter`, `structlog`, `python-dotenv`, `anthropic`,
  `exchange-calendars`, `pandas-ta`, `quantstats`, `vectorbt` all MISSING.
  (`numpy`, `pandas`, `scipy`, `pyyaml` survived.)
- Every CLI subcommand fails with `[cli] failed to build context: No module
  named 'requests'` (exit 4) because `main()` calls `_build_context()`
  unconditionally before dispatch — **including `git-sync`**, so the documented
  commit path is dead on bare `python3`.
- **The trader already discovered and worked around this.** Commit
  `2fbede78` (2026-06-16 09:47, `[trader 2026-06-16]`) message: *"ran via venv
  (bare python3 still broken)"*. There is now a working virtualenv at
  **`.venv/bin/python3` (Python 3.13.13)** with all deps installed. The trader
  has been running daily through it.
- **This run used `.venv/bin/python3 -m quant_trading_system.cli` for every CLI
  call, including the final `git-sync`.** The prompt's "There is no virtualenv"
  line is now stale.
- **Operator action required (HIGH):** repoint the Cowork scheduled tasks (both
  trader and this research task) and `daily_prompt`/`weekly_research_prompt` to
  `.venv/bin/python3`, OR reinstall deps into 3.14, OR pin Homebrew Python. Until
  then every scheduled run starts by hitting a dead interpreter. The news
  pipeline has ALSO been down since ~6/11 (brief stale at 6/15, no 6/16 brief) —
  almost certainly the same outage hitting the news task.

## Gaps identified this week (from trader's tasks.md + handoff)

The trader logged these under "Library gaps for the research agent". Triaged by
whether they are addressable via `propose-strategy` (the only ADD path I have):

| Gap | Type | Disposition |
|---|---|---|
| `volatility_regime` registry coverage hole (VIX broke <20) | tagging | **CLEARED** — tagged 4 options strategies (see §1) |
| `_load_news_brief()` no staleness guard (stale brief fed as live) | code fix | Out of scope (code, not strategy). Escalate to operator. Now acute given 6/11–6/15 brief outage. |
| `has_positive/negative_signal` keyword detector too coarse (ORCL false-neg 6/10, AVGO false-pos 6/16) | code/overlay | Out of scope. Needs `has_asymmetric_signal()` + catalyst-strength gate in strategy_runtime, not a new strategy. |
| `macro_event_window` overlay (FOMC dot plot 6/16–17) | overlay | Out of scope — overlay primitive, not standalone. Open-q #2. |
| AI-policy / export-control shock overlay | overlay | Out of scope. |
| `underwriter_franchise_event` (JPM; SpaceX/OpenAI listings) | overlay | Out of scope. |
| `m_a_arbitrage_event` (NUVL/GSK) | event strategy | Conceivably standalone, but event-driven → news_signal not replayable in backtester → 0 trades → REJECT. Not built. |
| AAPL WWDC / named multi-day `event_window_posture` | overlay | Out of scope. |
| AI-cohort multiple-compression overlay | overlay | Out of scope. |

**Net:** of 9 logged gaps, 1 was a tagging artifact I closed cleanly; the other
8 are either code fixes or event/overlay primitives that fall outside the
addition-battery architecture (research_tasks.md open-question #2 — still
unanswered by operator). `state/library_gaps.md` is empty (`gaps: []`) and
`list-active` shows `unclaimed_count: 0`, so there are **no symbol-level gaps**.

## §1. Metadata fix — closed the `volatility_regime` coverage hole

**Finding:** `gap-registry` reported `coverage_holes: ["volatility_regime"]`. Root
cause: the registry (`templates/registry.py`) scans all non-archived strategies
and reads `gap_types:` frontmatter, but **all 8 options strategies had no
`gap_types:` field** — so the obvious volatility responders (`iron_condor_high_iv`
with VIX≥18/IV-rank≥50, etc.) were invisible to the diagnostic. The hole was a
labeling artifact, not a capability gap.

**Safety check before editing:** confirmed `triage_symbol` (agent_tools.py:1444)
filters to `sf.type == "equity"`, so tagging options strategies does NOT leak
them into the trader's equity claim/triage path. The edit only affects the
`gap-registry`/`inverted_registry` diagnostic. Low-risk, in-scope metadata
hygiene (no strategy logic / no symbol claims changed → no battery required).

**Applied** — added `gap_types:` to:
- `iron_condor_high_iv` → `[volatility_regime]` (short-vol, VIX-gated, IV-rank≥50)
- `jade_lizard` → `[volatility_regime]` (short-vol, IV-rank, skew)
- `calendar_spread` → `[volatility_regime]` (IV term structure / vega)
- `long_straddle_earnings` → `[earnings_window, volatility_regime]` (long-vol pre-earnings)

**Verified:** `gap-registry` now reports `coverage_holes: []`; `volatility_regime`
has 4 responders and `earnings_window` gained a 2nd (`long_straddle_earnings`).
This directly clears the trader's logged "vol_regime_shift coverage hole" gap.
(Left the directional/income options — bull_put, bear_call, covered_call_wheel,
protective_put — untagged to avoid over-claiming; the hole is closed accurately.)

## §2. Archive sweep — all KEEP

Ran `evaluate-archive` on all 11 active equity strategies. All → **KEEP**.
Notables:
- `equity_trend_following_ema_cross`: rolling_sharpe 1.103, PSR 0.564, 7 trades in
  90d window — healthy.
- `equity_event_driven_catalyst`: now has lifetime order activity (ORCL buy 6/10,
  AVGO buy 6/16) but `trades_in_rolling_window: 0` and `rolling_sharpe: null` —
  no *closed* trades yet, and not in a 60-day-zero window
  (`actions_in_zero_trades_window: 3`). KEEP, correctly.
- Other 9: KEEP (insufficient evidence — first-pass claims, mostly unfired).

Options strategies have no equity-paper journal history, so they're no-evidence
KEEP by definition — not run.

## §3. Head-to-head validation of first-pass / provisional claims

Trader requested validation of the 5 first-pass + 3 provisional character-match
assignments. Ran each current claimant vs the universal challenger
`equity_trend_following_ema_cross` over a 2-yr window (2024-06-16 → 2026-06-13).

| Symbol | Claimant | Claimant Sharpe / trades | Trend Sharpe / trades | Winner | Action |
|---|---|---|---|---|---|
| META | macd_histogram | **0.043 / 16** | 0.0 / 0 | macd_histogram | **claim validated** |
| MRVL | breakout_volume | **1.313 / 8** | 0.0 / 0 | breakout_volume | **claim validated** |
| HPE  | rsi_divergence | 1.200 / 3 | 0.0 / 0 | rsi_divergence | claim holds (thin: 3tr) |
| CSCO | mean_rev_bollinger | 0.472 / 1 | 0.0 / 0 | mean_rev_bollinger | claim holds (thin: 1tr) |
| ORCL | event_driven | 0.0 / 0 | -0.434 / 1 | event_driven | claim holds (degenerate) |
| INTC | breakout_volume | 0.391 / 14 | 0.794 / 1 | trend_following | **NOT applied — trend's 1-trade fluke vs breakout's solid 14tr** |
| MSFT | macd_histogram | **-0.274 / 13** | 0.0 / 0 | trend_following | **NOT applied — degenerate (trend 0 trades); but macd is a NEGATIVE fit** |
| ARM  | breakout_volume | **-1.154 / 12** | 0.0 / 0 | trend_following | **NOT applied — degenerate; breakout is a NEGATIVE fit** |
| AVGO | event_driven | 0.0 / 0 | 0.0 / 0 | trend_following | **NOT applied — both silent (drawdown tiebreak)** |
| MU   | event_driven | 0.0 / 0 | 0.0 / 0 | trend_following | **NOT applied — both silent** |
| DELL | sector_rotation | 0.0 / 0 | 0.0 / 0 | trend_following | **NOT applied — both silent** |

**Why no reassignments were applied:** every verdict naming `trend_following` is
degenerate. `trend_following` does **0 trades** on all of MSFT/ARM/AVGO/MU/DELL
over 2 years (it "wins" purely on the Sharpe=0 ≥ negative-Sharpe / smaller-drawdown
tiebreak — i.e. it wins by inaction), and on INTC it "wins" on a single trade,
which has no statistical meaning. Reassigning a symbol from an *active* responder
to a *silent* one is strictly worse for coverage and is exactly the degenerate
0-trade-tiebreak failure the manual's 2026-06-06 recent-feedback and
research_tasks open-question #3 warn against. The manual is explicit: "Do NOT
make add-active/remove-active changes based on [degenerate adjudication]." So the
active set is **unchanged**.

Note this is *validation* of uniquely-held claims, not *conflict resolution* for
a newly-proposed strategy — the "apply head-to-head verdicts verbatim" mandate in
the manual is scoped to the latter (two strategies contesting the same symbol);
here there is no overlap (`unclaimed_count: 0`, no conflicts).

**Two genuine negative-fit findings (flagged, not acted on):**
- **MSFT under `equity_momentum_macd_histogram`: Sharpe −0.274 over 13 trades.**
  macd actively loses money on MSFT. Not a placeholder-silence problem — a real
  bad fit. But `trend_following` (0 trades) is not the fix.
- **ARM under `equity_breakout_volume_confirmation`: Sharpe −1.154 over 12
  trades.** Breakout actively loses on ARM. Same conclusion.
  These two are candidates for a *future dedicated responder*; they should NOT be
  dumped on `trend_following` just to satisfy a degenerate verdict. Carried to
  next Saturday.

**Structural finding:** `equity_event_driven_catalyst` enters on
`news_brief.has_positive_signal(...)`, a signal the backtester **cannot replay**.
It therefore shows **0 trades in every backtest/head-to-head** (AVGO/MU/ORCL),
making it structurally un-adjudicable by head-to-head. Its real-world fires
(ORCL 6/10, AVGO 6/16) don't appear in the simulator. Any head-to-head involving
it is uninformative by construction — stop re-running these expecting signal.

## §4. Candidate research — VIX-regime equity (considered, not added)

Per manual workflow step 3, searched for a battery-eligible standalone equity
strategy in the one logged-gap-adjacent area that is *price-based* (hence
backtestable, unlike the news/event overlays): volatility-regime equity timing.

**Searched:** "VIX volatility regime filter equity strategy backtest rules timing
2025" (WebSearch, 2026-06-16).

**Read / candidate class found:**
- VIX vs 200-day-MA regime switch (100% SPY when VIX<200MA, 50% SPY/50% cash
  above): ~9.8% ann. vs 9.2% buy-and-hold, max DD −22% vs −55% (2005–2025).
- VIX-Rank-gated RSI2 (enter only when VIX-Rank in a band): profit factor 2.52
  vs 1.64 unfiltered.
- Four-quadrant VIX regime gate on RSI2 for ES/NQ/RTY: ~+34% risk-adjusted.

**Verdict: REJECT pre-battery (not implemented).** Two independent disqualifiers:
1. **Architecture.** The high-signal versions are *regime overlays/gates on
   existing strategies* (VIX-gate on RSI2; allocation switch), not standalone
   entry/exit logic. That's the same overlay category as the other logged gaps —
   outside `propose-strategy` scope (open-question #2).
2. **The `num_trades ≥ 20` floor.** As a standalone single-symbol strategy, a
   VIX-MA crossover fires only ~4–8 times per 2-year window (regime shifts are
   rare); a VIX-Rank-gated reversion on one symbol tops out well under 20 (cf.
   the 2026-06-06 feedback: most strategies do 0–16 trades per symbol over 2yr).
   It would REJECT on trade count exactly as `macd_v3` did last week (16<20).
   Also: it would trade SPY, already claimed by `trend_following`.

Did not fabricate a strategy.py to run a battery I already know rejects — the
manual forbids running batteries "just to generate activity." The 3-week-old
structural blocker remains the binding constraint on every realistic candidate.

**Sources:**
- [QuantifiedStrategies — Volatility Trading Strategies](https://www.quantifiedstrategies.com/volatility-trading-strategies/)
- [Options Cafe — Momentum RSI + VIX filter backtest](https://options.cafe/blog/momentum-rsi-strategy-backtest-results/)
- [Volatility Box — Volatility Regime Detection](https://volatilitybox.com/research/volatility-regime-detection/)
- [SOA — Bond/Equity Volatility Indices for Allocation (2025-09)](https://www.soa.org/sections/investment/investment-newsletter/2025/september/rr-2025-09-bitalvo/)

## Open questions for next Saturday

1. **[NEW, HIGH] Interpreter outage.** Bare `python3`=3.14.5 has no harness deps;
   harness runs only via `.venv` (3.13). Until the operator repoints the
   scheduled tasks/prompts to `.venv/bin/python3`, every run starts broken and
   the news pipeline stays down. See §0.
2. **`num_trades ≥ 20` floor (3rd week unanswered).** The dominant blocker on
   every candidate. Suggest (b) multi-symbol aggregation in `simulate`.
3. **Head-to-head degenerate 0-trade tiebreak (open-q #3, unanswered).** This
   week it would have churned 6 symbols (MSFT/ARM/AVGO/MU/DELL/INTC) onto a
   silent strategy. Until "both 0 trades → winner: null" is implemented,
   head-to-head cannot validate first-pass claims.
4. **Event-overlay architecture (open-q #2, unanswered).** Most real gaps are
   overlays. No ADD path exists for them.
5. **`event_driven_catalyst` is un-backtestable** (news_signal not replayable);
   head-to-heads on its symbols are noise. Needs a backtest news-replay fixture
   or it can never be adjudicated.
</content>
</invoke>
